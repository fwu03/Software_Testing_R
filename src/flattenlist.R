#' Flattens a list so that elements in embedded lists are 
#' returned in a flat list
#' 
#' @param l A list
#' @return A flat list that has all of the elements in l 
flattenlist <- function(l) {
  
}